import { DBImage } from '../interfaces/db-image';

// Stores all images in library currently being viewed/edited by logged-in user

export const LibraryImagesData: { data: DBImage[] } = {data: undefined};
